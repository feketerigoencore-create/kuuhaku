---
type: ダッシュボード
作成日: 2026-06-24
tags: [管理, ブログ]
---

# ブログ ダッシュボード（全ジャンル横断）

このノートは **Dataview プラグイン** で、`#記事` タグの付いた記事ノートを全ジャンル横断で自動集計します。
記事を増やしても手で更新は不要です（各 vertical の `記事/` 配下に、`type: 記事`・`tags: [記事]`・優先度スコア等のフロントマター付きノートを足すだけ）。

> [!info] 使うには
> 設定 → コミュニティプラグイン → **Dataview** をインストールして有効化してください。
> 有効化すると下のコードブロックが表が表に変わります。

---

## 優先度ランキング（全記事）

```dataview
TABLE WITHOUT ID
  link(file.link, ニッチ) AS "ニッチ",
  優先度スコア AS "優先度",
  ステータス AS "状態"
FROM #記事 AND -"_archive"
SORT 優先度スコア DESC, file.name ASC
```

---

## ステータス別カンバン

### アイデア
```dataview
LIST 優先度スコア
FROM #記事 AND -"_archive"
WHERE ステータス = "アイデア"
SORT 優先度スコア DESC
```

### 下書き中
```dataview
LIST 優先度スコア
FROM #記事 AND -"_archive"
WHERE ステータス = "下書き"
SORT 優先度スコア DESC
```

### 公開済み
```dataview
TABLE WITHOUT ID
  link(file.link, ニッチ) AS "ニッチ",
  公開URL AS "URL"
FROM #記事 AND -"_archive"
WHERE ステータス = "公開"
SORT file.name ASC
```

---

## 最有力候補（スコア16以上）

```dataview
TABLE WITHOUT ID
  link(file.link, ニッチ) AS "ニッチ",
  優先度スコア AS "優先度",
  差別化 AS "差別化点"
FROM #記事 AND -"_archive"
FLATTEN スコア.差別化 AS 差別化
WHERE 優先度スコア >= 16
SORT 優先度スコア DESC
```

---

## キーワード一覧（記事横断）

```dataview
TABLE WITHOUT ID
  link(file.link, ニッチ) AS "ニッチ",
  狙いキーワード AS "狙いKW"
FROM #記事 AND -"_archive"
SORT 優先度スコア DESC
```
